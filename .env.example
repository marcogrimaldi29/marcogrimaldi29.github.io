# Copy to .env for local dev. In CI this is provided via a GitHub Actions secret
# and exposed to the build as an env var (replaces the old sed injection).
# Umami analytics website ID (public by nature; gated to production builds).
PUBLIC_UMAMI_WEBSITE_ID=e5363bda-3e67-41f1-b38b-31188d781897
